# SUGGESTED_AGENTS.md

这些内容适合长期保存在项目级 agent 指南中。不要把一次性任务需求直接写入长期规则。

## 工作方式
- 开始编辑前，先读取相关说明文件并给出简短计划。
- 遇到高影响不确定性时，最多提出 3 个关键问题；否则用明确假设继续。
- 不要默认读取 `.env`、私钥、token、数据库 dump 或生产日志。
- 完成后报告修改文件、关键决策、验证结果和剩余风险。

## 上下文卫生
- 一次性任务需求应保存在任务 spec 中。
- 长期规则应短、稳定、可复用。
- 过期假设应从新 session 中移除。
